name: Website retired — README is primary
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Website publication is disabled. Existing URLs only redirect to the GitHub README. See docs/README_PUBLICATION.md.'
