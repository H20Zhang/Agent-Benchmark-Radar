name: Validate Benchmark Radar

on:
  push:
    branches: [main, research-radar-reading-v1]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Test time-first contract
        run: python -m unittest discover -s tests -v
      - name: Validate bilingual entry and registry
        run: python scripts/validate_reading.py
      - name: Validate benchmark detail pages
        run: python scripts/validate_detail_pages.py
      - name: Audit benchmark detail depth
        run: python scripts/audit_detail_pages.py
      - name: Install web dependencies
        working-directory: web
        run: npm install --no-audit --no-fund
      - name: Test web contracts
        working-directory: web
        run: npm test
      - name: Check Astro source
        working-directory: web
        run: npm run check
      - name: Build Astro site
        working-directory: web
        run: npm run build
